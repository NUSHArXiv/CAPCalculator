package com.thepyprogrammer.capcalc.ui.modules.adapter

import android.widget.Spinner
import android.widget.TextView
import androidx.cardview.widget.CardView
import androidx.recyclerview.widget.RecyclerView
import com.thepyprogrammer.capcalc.R
import com.thepyprogrammer.capcalc.model.data.CAPModule

class ModuleViewHolder(itemView: CardView) : RecyclerView.ViewHolder(itemView) {
    // val codeView: TextView = view.findViewById(R.id.content)
    // val nameView: TextView = view.findViewById(R.id.desc)
    // val capselector: Spinner = view.findViewById(R.id.capselector)
    // var module: CAPModule? = null
}